= haml(:new, :layout => false)

#box
  %table
    %th Description
    %th Hours
    - @tasks.each do |t|
      %tr
        %td= t.desc
        - if t.is_completed?
          %td.center= "%.2f" % t.time_worked
        - else
          %td.center
            %a{:href => "/e/#{t.id}"}
              %img{:src => "/images/stop.png"}
    %td
      %p
        %strong Total time worked:
    %td.center= "%.2f" % @total

#footer
  %a{:href => "/clear"} clear all
