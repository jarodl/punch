= haml(:new, :layout => false)

%table
  - @tasks.each do |t|
    %tr
      %td= t.desc
      - if !t.ended_at.nil?
        %td= t.time_worked
      %td
        - if t.ended_at.nil?
          %a{:href => "/e/#{t.id}"} stop
